//TODO TODO

module multiplier (
    input a[32],
    input b[32],
    output mul[32]
  ) {
  //Disabling Warnings/Errors
  always{  
    mul = a^b; //This is only here to disable WARNINGS
  }
}
